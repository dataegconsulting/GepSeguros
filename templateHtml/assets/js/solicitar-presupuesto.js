// Placeholder JS – listo para conectar backend más adelante
document.addEventListener("submit", (e) => {
  if (e.target.matches(".quote-form")) {
    e.preventDefault();
    alert("Solicitud enviada (demo). Conectar backend más adelante.");
  }
});